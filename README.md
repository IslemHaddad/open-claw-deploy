# OpenClaw Deploy Options

This repository provides three deployment options for OpenClaw:

- `linux-direct/` - clone and run OpenClaw directly on Linux.
- `docker-swarm/` - deploy OpenClaw as a Docker Swarm stack.
- `kubernetes/` - deploy OpenClaw to Kubernetes in a configurable namespace.

Each option uses a `deploy.sh` entrypoint script.
